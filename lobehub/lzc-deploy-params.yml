# lzc-deploy-params.yml - LobeChat 部署参数配置

params:
  - id: PROXY_URL
    type: string
    name: PROXY_URL
    description: 用于指定连接到外部服务的代理 URL。（示例：http://127.0.0.1:7890 或 socks5://localhost:7891）
    optional: true
    hidden: false

locales:
  zh:
    PROXY_URL:
      name: PROXY_URL
      description: 用于指定连接到外部服务的代理 URL。（示例：http://127.0.0.1:7890 或 socks5://localhost:7891）
  en:
    PROXY_URL:
      name: PROXY_URL
      description: Used to specify the proxy URL for connecting to external services. (Example: http://127.0.0.1:7890 or socks5://localhost:7891)
